<launch>

  <param name="robot_description"
      command="$(find xacro)/xacro $(find hera_description)/robots/hera_full.urdf.xacro"/>

  <!-- Load joint controller configurations from YAML file to parameter server -->
  <rosparam file="$(find hera_description)/config/yaml/s_c.yaml" command="load"/>

  <!-- load the controllers -->
  <node name="controller_spawner" pkg="controller_manager" type="spawner" respawn="false"
	output="screen" ns="/suport" args="suporte_state_controller
                      suporte_position_controller
                      "/>

  <!-- convert joint states to TF transforms for rviz, etc -->
  <node name="suport_state_publisher" pkg="robot_state_publisher" type="robot_state_publisher"
	respawn="false" output="screen">
    <remap from="/joint_states" to="/suport/joint_states" />
  </node>

</launch>